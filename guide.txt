info@lumytics.com

This is the right stage to move from **unit testing** to **integration testing**. At this point you've individually verified:

* ✅ DraftGenerator
* ✅ MarginUncertaintyScorer
* ✅ ThresholdCalibrator
* ✅ RetrievalGate
* ✅ VectorRetriever

Now the goal is to verify that they work together as a TARG pipeline.

## Stage 1 – Happy path (end-to-end)

Create the components:

```python
draft_generator = DraftGenerator(...)
scorer = MarginUncertaintyScorer(beta=3.0)

calibration = ThresholdCalibrator.load(
    "threshold.json"
)

gate = RetrievalGate(calibration)

retriever = VectorRetriever(vector_store)

answer_generator = AnswerGenerator(...)
```

Then run a query that you know exists in your vector database, for example:

```python
query = "What is Apache Struts2?"
```

The expected execution flow is:

```text
User Query
      │
      ▼
DraftGenerator
      │
      ▼
MarginUncertaintyScorer
      │
      ▼
RetrievalGate
      │
      ├────────── retrieve=False ─────────► AnswerGenerator
      │
      ▼
VectorRetriever
      │
      ▼
AnswerGenerator
```

Verify that:

```python
pipeline.run(query)
```

returns an answer without any exceptions.

---

# Stage 2 – Verify gate behaviour

Choose two queries.

### Low uncertainty

A question the model already knows.

Example:

```text
What is Python?
```

Expected:

```text
retrieve == False
documents == []
```

---

### High uncertainty

Something that only exists inside your RAG corpus.

Example:

```text
Explain CVE-2017-5638.
```

Expected:

```text
retrieve == True

len(result.documents) > 0
```

This verifies the gate is actually controlling retrieval.

---

# Stage 3 – Verify retrieved documents

After

```python
documents = retriever.retrieve(...)
```

inspect:

```python
for d in documents.documents:

    print(d.id)

    print(d.score)

    print(d.text[:150])
```

Ensure

* IDs are correct
* scores are reasonable
* retrieved text is relevant

---

# Stage 4 – Compare both execution paths

Run the same question twice.

First force

```python
retrieve=False
```

Second force

```python
retrieve=True
```

Compare

* answer quality
* retrieved documents
* latency

This demonstrates the benefit of adaptive retrieval.

---

# Stage 5 – Stress tests

Try:

```python
"What is Kubernetes?"

"Explain CVE-2021-44228"

"asdfghjkl"

""

"          "

"What is Apache Struts2?"
```

Ensure the pipeline behaves predictably:

* validation errors for invalid input,
* successful retrieval for relevant queries,
* graceful handling when no documents are found.

---

# Stage 6 – Logging

At this stage, log each component's output. For every query, record something like:

```text
========================================
Query:
What is Apache Struts2?

Draft:
Apache Struts2 is...

Uncertainty:
0.47

Threshold:
0.31

Retrieve:
True

Retrieved:
3 documents

Top score:
0.91

Final answer:
...
========================================
```

This makes it much easier to diagnose issues in later experiments.

---


=========
stage 3
=========

Stage 3 – Retrieval Verification
Objective

Verify that the vector store returns the correct evidence for representative questions before evaluating TARG.

Step 3.1 — Build a representative corpus

    Collect authoritative PDFs.
    Chunk documents consistently.
    Attach metadata (source, page, chunk, id).
    Build the vector store.
    Verify indexing before running TARG.

Step 3.2 — Preserve source metadata

Step 3.3 — Prepare benchmark queries

Step 3.4 — Retrieval inspection

Step 3.5 — Manual relevance assessment

Step 3.6 — Measure retrieval accuracy

========
Step 3.3
========

Therefore, the minimum mandatory code changes are:

retrieval_verification.py
retrieval_models.py
retrieval_inspector.py
the verification experiment/run script
any result serializer or tests using the old metric names

=======
Testing
=======

Let's proceed to measuring retrieval accuracy as against benchmark_queries.json by computing metrics such as: Top-1 document accuracy, Top-1 page accuracy, Recall@5 and MRR (Mean Reciprocal Rank). The targ_code.txt file contains all the code used in the project so and the official targ research paper for your reference.

Yes. This is actually the next experiment in the reproduction pipeline, and it should be completely independent of TARG itself. The paper first verifies that the retriever is capable of finding the correct evidence before introducing adaptive gating. Your uploaded implementation already hints at this separation through retrieval_verification.py, although it currently only measures source-level Top-1 accuracy, Hit@K, and Mean First Correct Rank.

=========
Next move
=========

- Running the LLM on a GPU instead of a CPU.
- Using a smaller or quantized model.
Parallelising benchmark evaluation across multiple processes or GPUs.
- Reducing the number of benchmark queries during development.
Caching retrieved documents and repeated model outputs where appropriate.
Optimising retrieval (e.g., batching embedding operations or tuning FAISS settings).


========
Commands
========

Run the full workflow

 python main.py run \
  --question "What is DNS amplification?"

Run each stage separately

 python main.py prepare
 python main.py calibrate --strategy accuracy
 python main.py ask --question "What is DNS amplification?"

Budget calibration

 python main.py calibrate \
  --strategy budget \
  --target-retrieval-rate 0.30

Interactive inference
 python main.py ask

Enable parallel calibration 
 
 python main.py calibrate --parallel --device auto --workers 4

On a multi-GPU system
 python main.py calibrate --parallel --device cuda --gpu-ids 0 1